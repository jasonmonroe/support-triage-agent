[tool.ruff]
line-length = 79

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[project.optional-dependencies]
test = [
  "pytest>=7.0",
  "pytest-mock",
]
